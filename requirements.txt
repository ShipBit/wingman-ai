aiofiles==24.1.0
azure-cognitiveservices-speech==1.48.1
backports-tarfile>=1.0.0
edge-tts==7.2.7
elevenlabslib==0.32.1
fastapi==0.128.6
faster_whisper==1.2.1
google-genai==1.62.0
httptools==0.7.1
hume==0.13.6
markdown==3.10.2
mcp==1.26.0
numba==0.63.1
numpy==2.3
openai==2.14.0
packaging==24.2
pedalboard==0.9.22
platformdirs==4.5.1
pyaudio==0.2.14
pydantic==2.12.5
pydirectinput-rgx==2.1.3
pyinstaller==6.18.0
pygame==2.6.1
python-multipart==0.0.21
PyYAML==6.0.3
rapidfuzz==3.14.3
requests==2.32.5
scipy==1.17.0
setuptools==75.8.0
show-in-file-manager==1.1.5
sounddevice==0.5.5
soundfile==0.13.1
SpeechRecognition==3.14.5
typing_extensions==4.15.0
uvicorn==0.40.0

# setuptools vendored dependencies - required for PyInstaller bundling
jaraco-context>=6.0.0
jaraco-functools>=4.0.0

# NVIDIA CUDA libraries for GPU-accelerated FasterWhisper (ctranslate2)
# These are bundled with PyInstaller to provide zero-config GPU support
# IMPORTANT: Must stay on CUDA 12.x (12.4-12.8 tested). CUDA 13.x is NOT compatible with ctranslate2.
# Not available on macOS — CUDA is Windows/Linux only.
nvidia-cublas-cu12>=12.4.2,<13.0; sys_platform != 'darwin'
nvidia-cuda-runtime-cu12>=12.4.99,<13.0; sys_platform != 'darwin'
nvidia-cudnn-cu12>=9.1.0,<10.0; sys_platform != 'darwin'
nvidia-cuda-nvrtc-cu12>=12.4.99,<13.0; sys_platform != 'darwin'

# Local AI (llama-server binary is auto-downloaded)
tiktoken>=0.9.0

# Parakeet STT (NVIDIA Parakeet TDT via ONNX Runtime)
onnx-asr>=0.3.3
onnxruntime-gpu>=1.19.0; sys_platform == 'win32' or sys_platform == 'linux'
onnxruntime>=1.19.0; sys_platform == 'darwin'
pyobjc-framework-Quartz>=10.0; sys_platform == 'darwin'

# Pocket-TTS
pocket-tts==2.0.0
torchao==0.17.0 # required for int8 quantization; torch.ao fallback breaks voice cloning
torch==2.8.0 # don't upgrade!
torchaudio==2.8.0 # don't upgrade!

# =============================================================================
# SKILL DEPENDENCIES
# These are requirements from built-in skills. TODO: Refactor skills to reduce deps.
# =============================================================================

# --- api_request skill ---
aiohttp>=3.11.12

# --- vision_ai / auto_screenshot skills ---
mss>=10.0.0
pillow>=11.1.0
PyGetWindow>=0.0.9
PyRect>=0.2.0

# --- spotify skill ---
spotipy>=2.25.0

# --- file_manager skill ---
cryptography>=43.0.1
pdfminer.six>=20240706

# --- control_windows skill ---
clip-util>=0.1.27

# --- msfs2020_control skill ---
SimConnect>=0.4.26

# --- ats_telemetry skill ---
pyproj>=3.6.1
truck-telemetry>=0.0.3